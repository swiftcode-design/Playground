import TweenMax, { TimelineMax } from 'gsap';

export const tl = new TimelineMax({repeat:2, repeatDelay:1});

export const animateFlash = new TimelineMax({repeat:0});

export const hamburgerAnime = new TimelineMax();

export const angularAnime = new TimelineMax();
